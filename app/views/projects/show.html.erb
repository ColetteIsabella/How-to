<div id='project'>
  <h2><%= @project.title %></h2>
  <%= link_to "Edit Project", [:edit, @topic, @project] %>

  <%= link_to "Delete Project", [@topic, @project], :method => :delete,
       :confirm => "Are you sure you want to delete this project?" %>

  <%= simple_format(@project.description) %>
</div>
